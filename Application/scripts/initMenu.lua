local entity = scene.CreateEntity()
scene.SetComponent(entity, "behaviour", "scripts/playButton.lua")

entity = scene.CreateEntity()
scene.SetComponent(entity, "behaviour", "scripts/editorButton.lua")